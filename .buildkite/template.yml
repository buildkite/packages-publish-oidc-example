name: "Packages Registries OIDC Publish Example"
description: "An example pipeline that builds an Ubuntu package and then publishes it to a registry using an OIDC token."
emoji: ":debian:"
languages:
  - "Shell"
steps:
  - command: "buildkite-agent pipeline upload"
    label: ":pipeline:"
